<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.apppayments">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application

        android:name=".Helpers.DataGlobal"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppPayments"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".ListTransactionsActivity"
            android:exported="false"
            android:label="@string/title_activity_list_transactions"
            android:theme="@style/Theme.AppPayments" />

        <activity
            android:name=".DetailTransactionActivity"
            android:exported="true"
            android:label="@string/title_activity_detail_transactions" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>